#include "ui.h"

int init_ui(ctrl* c)
{
	//check all the inputs and set initial values
	//.pot = READ PIN;
	//a.swi = READ PIN;
	//a.pos[0], a.pos[1] = 0;
	
	return 0;
}

int ui_input(ctrl* c)
{
	//check button
	//u.pot = READ PIN;
	//u.swi = READ PIN;
	
	return 0;
}

int ui_output(ctrl* c)
{
	//calculate and output stylus position
	/*
	
	spi_output(a.pos[0], a.pos[1]);
	
	
	*/
	
	return 0;
}

